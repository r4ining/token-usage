# MySQL database connection (pointing to the new-api database)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=new-api
DB_TABLE_NAME=logs

# Service port (default: 8080)
PORT=8080

# Directory for storing price configuration (mapped as a volume)
DATA_DIR=/data
